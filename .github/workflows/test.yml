# This is an empty workflow.
name: ci

on:
  push:
    branches: [develop]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:     
      - name: Run tests
        uses: actions/checkout@v6

        name: setup node.job
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: 'npm'